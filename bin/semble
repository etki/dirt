#!/usr/bin/env ruby

library_root = File.expand_path('../../lib', __FILE__)
$:.unshift(library_root) unless $:.include?(library_root)

require 'cli/entry_point'

exit Semble::CLI::EntryPoint.new.run(ARGV)